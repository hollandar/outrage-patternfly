@using Outrage.Patternfly.Components.Utilities

@inherits Outrage.Patternfly.Components.Base.PatternflyComponentBase

<div class="@Classes" @attributes="AdditionalAttributes">
    @ChildContent
</div>

@code {
    [Parameter] public BannerColor Color { get; set; } = BannerColor.Default;
    [Parameter] public RenderFragment? ChildContent { get; set; }

    protected override IEnumerable<string> OnIntroduceClasses()
    {
        var classes = new HashSet<string>();
        classes.Add("pf-v5-c-banner");
        switch (Color)
        {
            case BannerColor.Info: classes.Add("pf-m-blue"); break;
            case BannerColor.Success: classes.Add("pf-m-green"); break;
            case BannerColor.Warning: classes.Add("pf-m-gold"); break;
            case BannerColor.Danger: classes.Add("pf-m-red"); break;
            case BannerColor.Default:
            default: break;
        }
        return classes;
    }
}
